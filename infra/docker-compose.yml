version: "3.9"

services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    image: ssd-app:dev
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "no"]
